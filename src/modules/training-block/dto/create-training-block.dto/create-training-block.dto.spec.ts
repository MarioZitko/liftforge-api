import { CreateTrainingBlockDto } from './create-training-block.dto';

describe('CreateTrainingBlockDto', () => {
  it('should be defined', () => {
    expect(new CreateTrainingBlockDto()).toBeDefined();
  });
});
