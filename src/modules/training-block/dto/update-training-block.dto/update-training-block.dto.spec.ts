import { UpdateTrainingBlockDto } from './update-training-block.dto';

describe('UpdateTrainingBlockDto', () => {
  it('should be defined', () => {
    expect(new UpdateTrainingBlockDto()).toBeDefined();
  });
});
