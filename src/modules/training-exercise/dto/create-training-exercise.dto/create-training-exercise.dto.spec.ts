import { CreateTrainingExerciseDto } from './create-training-exercise.dto';

describe('CreateTrainingExerciseDto', () => {
  it('should be defined', () => {
    expect(new CreateTrainingExerciseDto()).toBeDefined();
  });
});
